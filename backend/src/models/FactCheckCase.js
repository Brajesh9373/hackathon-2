const mongoose = require('mongoose');
module.exports = mongoose.model('FactCheckCase', new mongoose.Schema({ complaint_id: { type: mongoose.Schema.Types.ObjectId, ref: 'Complaint', index: true }, claim_cluster_id: { type: mongoose.Schema.Types.ObjectId, ref: 'ClaimCluster' }, claim: String, status: { type: String, enum: ['PENDING', 'SUPPORTED', 'CONTRADICTED', 'UNVERIFIED', 'DISMISSED'], default: 'PENDING' }, signals: [String], sources: [{ publisher: String, official_url: String, digest: String }], reviewer_id: { type: mongoose.Schema.Types.ObjectId, ref: 'User' }, reviewer_reason: String, resolved_at: Date }, { timestamps: true }));
